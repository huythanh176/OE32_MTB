<%= form_for current_user.comments.new, url: movie_comments_path(@movie.id) do |f| %>
  <div class="col-sm-10">
    <%= f.text_area :content, class: "form-control" %>
  </div>
  <%= f.submit t("comments.create.btn"), class: "btn btn-primary col-sm-2" %>
<% end %>
